#include <stdio.h>
#include "main.h"

/**
 *_abs - prints theabsolute value of an intiger
 *Description: prints the absolute value of an int
 *@i: the value to test
 *Return: The absolute value of i
 *
 *
 */
int _abs(int i)
{
return (i < 0 ? -1 : 1);
}
